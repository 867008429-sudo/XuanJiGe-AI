# 玄机阁 P2 人格层与香火计价进度

日期：2026-09-03

## 本次目标

围绕“用户可自主选择不同风格道长”的产品方向，把通用追问能力升级为带人格、模型档位和香火计价的命理追问产品形态。核心原则保持不变：风格可变，事实不变。

## 已完成进度

### M1 人格注册表

- 新增 `chat_personas.py`，集中维护五位道长的白名单、道号、简介、风格 prompt、模型档位参数和香火定价。
- 当前阵容为：玄真散人、清虚道长、白云师太、铁口神算、掌门真人。
- 默认人格为 `qingxu`，旧客户端不传 `persona` 时保持兼容。
- 风格区统一前置自缚条款，避免个性化表达突破健康、投资、婚姻等安全边界。

### M2 香火计量

- 将追问额度从“每盘 5 次”迁移为“每盘 10 炷香”。
- `chat_followups.used` 保持字段不变，但语义改为已消耗炷数。
- 扣费函数支持按人格价格原子扣减，失败时按实扣炷数退款。
- `chat_requests` 新增 `price` 与 `persona` 字段，幂等回放保留当次道长和扣费信息。
- 存量迁移采用显式事务和列检测，避免重复迁移导致错账。

### M3 路由与模型档

- `/api/chat` 新增可选 `persona` 参数，使用注册表白名单校验，未知人格直接返回 400。
- 路由将选中的 persona 传入 LangGraph，图编排层按 persona 选择模型名、temperature 和 max_tokens。
- 香火不足时返回明确文案，例如“请教掌门真人需 5 炷香，当前余额 3 炷”。
- SSE `done` 事件扩展 `price`、`persona`、`cached` 和 `request_id`，前端可直接展示账目提示。
- 新增 `/api/chat/personas`，返回有序道长卡片数据、价格、当前人格和余额可负担状态。

### M4 前端集成

- 在追问区顶部新增道长选择条，卡片展示道号、简介和炷香价格。
- 当前选中卡片高亮，余额不足的人格置灰。
- 切换道长后，后续 `/api/chat` 请求会携带选中的 `persona`。
- 追问文案从“次数”统一切换为“香火 / 炷香”。
- 追问成功后追加账目提示，例如“请教铁口神算，上香 2 炷”。
- 403 分支读取后端返回的详细香火不足文案并展示给用户。

## 当前验证结果

- 后端人格、路由、数据库迁移和图编排测试已补充覆盖。
- 重点覆盖：未知 persona 拒绝、空 persona 默认、掌门 5 炷扣费、混合人格扣费、幂等回放携带原始人格与价格、`/api/chat/personas` 的 current/affordable/ownership 逻辑。
- 本轮提交前已重新执行全量 Python 回归：149 项测试通过；前端内联脚本已抽取并通过 `node --check` 语法校验。

## 未完成与下一步

- M4 还需补一轮浏览器手工冒烟：确认五位卡片顺序、高亮、置灰、切换提示、请求 payload 与 403 文案。
- M5 待启动：跨人格事实一致性评测、医疗/投资/婚姻边界对抗、同一 thread 中途换道长的连续性评测。
- README 仍需进一步从“5 次追问”统一更新为“10 炷香火”的最终产品文案。

## 本次提交范围

- 后端：`chat_personas.py`、`chat_graph.py`、`db.py`、`app.py`
- 前端：`app.py` 内嵌 HTML/CSS/JS 的道长选择条与香火文案
- 测试：`tests/test_chat_personas.py`、`tests/test_chat_graph.py`、`tests/test_chat_db.py`、`tests/test_chat_route.py`、`tests/test_chat_tools.py`
- 文档：`persona-plan.md`、`agent-plan.md`、`SPIKE-REPORT.md`、本进度文件
