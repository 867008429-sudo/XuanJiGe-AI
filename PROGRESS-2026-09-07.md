# 玄机阁 Agent 化进度日志（2026-09-07）

## S15 P4-UI 去后台化修订

- 根据用户反馈，确认上一版“Agent Console”更像网站后台，不适合直接给 C 端用户看。
- 保留 Flask inline 前端与既有 DOM/JS 契约，把首页产品表达改为“命盘咨询间”：先建盘，再继续追问，底部说明如何提问、如何给依据和使用边界。
- 用户可见界面不再暴露 Agent Console、Grounding、P4、运行面板、内测指标、chunk_id、search_classics、checkpoint 或 issue code。
- 对抗性审查结论：可信度机制需要被翻译成用户语言；工程词保留在受保护后台、README 技术段落和测试中，不进入首屏体验。
- 定向验证已完成：`py_compile app.py tests/test_frontend_persona_contract.py` 通过；frontend persona contract 11 项、chat_feedback 7 项、chat_route 39 项通过。
- 运行检查已完成：`/health` 返回 200；PAT 常见前缀扫描无命中；`git diff --check` 仅提示 Windows 行尾；截图位于 `artifacts/ui-user-consultation/`。

## 当前下一步

- 进入 P4-2：生成/发放 50-100 个邀请体验码，记录激活、真实追问会话数、helpful 反馈与误导抽审率。
- 最后统一跑 full unittest、preflight、diff check、PAT 扫描与 smoke；公网 Docker 部署已复验，后续重点转向内测运营数据。

## S16 本地超级测试账号

- 新增 `accounts.is_superuser` 数据库标记；超级账号报告解读不消耗免费次数/credits，chat 追问不消耗每盘香火。
- 普通账号接口响应保持原契约；超级账号在 `/api/quota`、chat SSE done、`/api/chat/quota`、`/api/chat/history` 额外返回 `unlimited: true`，前端显示“超级账号 · 无限额度 / 香火无限”。
- 已创建/更新本地账号 `xjg_super`；密码未写入任何仓库文件。
- 定向验证：`py_compile db.py app.py tests/test_chat_db.py tests/test_chat_route.py` 通过；chat_db 29 项、chat_route 41 项、auth_security 3 项、frontend persona contract 11 项、registration_invite_flow 4 项通过；HTTP smoke 不调用大模型并确认无限额度。

## S17 Chat 空回复事故修复

- 复现用户截图问题：`acct:4:23` 下存在 2 条 `reply_text=''` 的 `chat_requests`，usage logs 显示模型实际调用成功且 completion tokens 接近 2000。
- 根因：chat 链路使用 `ChatDeepSeek` 时没有显式传 `thinking: disabled`，DeepSeek V4 流式事件先给 `additional_kwargs.reasoning_content`，正文 `content` 可为空；解析器正确不外发 reasoning，但路由把空 `reply_text` 当成功落库。
- 修复：`chat_graph.resolve_model_kwargs()` 为 chat 模型传 `extra_body={'thinking': {'type': 'disabled'}}` 与 `stream_usage=True`；`parse_stream_event()` 支持文本 content blocks，继续忽略 reasoning。
- 兜底：`/api/chat` 完成前要求 `reply_text.strip()` 非空；空回复记录 `empty_reply`、退还香火、不写幂等缓存，并通过 SSE error 告诉前端重试；前端只给非空助手回复挂 helpful 按钮。
- 本地数据：删除 `acct:4:23` 的 2 条空回复缓存及关联 feedback，保留 usage logs 作为事故审计线索；当前本地空 `chat_requests` 数为 0。
- 对抗性审查：reasoning 不泄漏、ToolMessage 原始 JSON 不外发、空回复不能回放/评价、普通用户退款、超级账号仍无限额度、旧空 done 不再显示计香/反馈入口。
- 验证：`py_compile chat_graph.py app.py tests/test_chat_graph.py tests/test_chat_route.py tests/test_frontend_persona_contract.py` 通过；chat_graph/chat_route/chat_db/frontend/chat_feedback 共 118 项通过；真实掌门档 smoke 返回非空正文且 `reasoning_seen=False`；重启后本地 `/api/chat` smoke 返回 15 个 token 片段和 `done`。

## S18 GitHub 发布与服务器上线

- GitHub 发布：`1319887 feat: build agent consultation loop` 已推送到 `origin/main`，远端 `refs/heads/main` 与本地 HEAD 一致。
- 上线前验证：`python -m unittest discover -s tests -p "test_*.py"` 共 210 项通过；`tools/preflight.py` required checks 通过，只有 `ADMIN_TOKEN` 未配置的 optional warn；仓库/暂存区敏感信息扫描未发现 PAT 或 API key。
- 服务器部署：旧 `/home/ubuntu/xuanjige` 非 Git 仓库，保留原目录、`.env` 与 Docker volumes；新代码克隆到 `/home/ubuntu/xuanjige_release`，使用 `docker compose -p xuanjige up -d --build` 复用既有 `xuanjige_*` volumes。
- 服务端验证：release HEAD 为 `1319887cc4c11f8852361836b7485ceb0e572235`；容器 `xuanjige` `running healthy`、重启次数 0；公网 `/health` 和 `/api/auth-config` 返回 200，首页已显示 `命盘咨询间`、空回复保护和超级账号文案。
- 对抗性审查：容器内 `chat_graph.py/chat_audit.py/chat_cleanup.py/data/classics` 存在；定向 `py_compile` 通过；运行时 checks 为 `ok`，`admin_stats` 因未配置 `ADMIN_TOKEN` 为 optional disabled；最近日志无启动错误，仅观察到外部扫描 `/admin/config.php` 返回 404。
- 服务端超级账号：已创建/更新 `xjg_super_test`；API smoke 验证登录 200、`/api/quota unlimited=true`、新测试命盘 `/api/chat/quota quota_left=999999999`，真实 `/api/chat` SSE 返回非空正文并 `done`，测试命盘已清理。
- 部署注意：服务器直接从 PyPI 下载依赖极慢，本次 Docker 构建依赖安装约 12 分钟；容器内完整 `tools/preflight.py` 一度挂住，已中断并改用带超时的定向编译、运行时、HTTP、日志检查。
