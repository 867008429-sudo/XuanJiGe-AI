# 玄机阁 P3 生产化继续推进

日期：2026-09-06

## 本次目标

在已有 P3 基础设施上继续补齐生产化验收：让“5% 离线抽审”能实际跑通，并把 chat token 用量从“暂未入账”推进到“有 usage_metadata 则入账”。

## 已完成进度

### S7 P3-3 离线抽审 v1

- 新增 `chat_audit.py`，提供本地 `heuristic-v1` 判官，覆盖 `RISKY_PHRASES` 与常见变体，如“稳稳翻倍”“闭眼入”“躺赚”“必离”“确诊”等。
- 新增 `chat_audit_logs` 表，只记录 verdict、issue code、reply_sha256、persona、sample_rate，不保存用户问题、回复正文、命盘 JSON 或生辰信息。
- 新增 `tools/audit_chat_replies.py`，默认按 5% 稳定哈希抽样，可 `--dry-run` 先看候选和判定结果。
- 删盘级联与 retention metadata cleanup 同步清理 `chat_audit_logs`，避免删除命盘后残留审计元数据。

#### 对抗性审查

- 抽审脚本本地读取 `chat_requests.reply_text`，但落库只存哈希与问题码，避免把隐私正文复制到第二张表。
- 同一 `(thread_id, request_id, judge)` 有唯一约束，重复运行不会重复审同一条。
- 当前是启发式判官 v1，不冒充 LLM-as-judge；后续可在相同表结构上升级判官实现。

### S8 chat token 机会性入账

- `/api/chat` 流式循环现在会读取 LangChain event 的 `usage_metadata`，提取 prompt/completion/total tokens。
- `usage_logs` 的 `/api/chat` 记录从固定 0 改为“有 usage_metadata 则记录 tokens 与估算成本；无则保持 0”。
- `chat_usage_logs` 同步新增 prompt_tokens、completion_tokens、total_tokens、cost_usd 字段，并补了旧库幂等迁移。

#### 对抗性审查

- usage-only chunk 不会作为 SSE token 外发，前端只收到正常正文与 done 事件。
- 多个 usage chunk 合并时取最大累计值，不做逐包累加，避免 DeepSeek/OpenAI 末包累计 usage 被双计。
- token 入账不影响扣香火、退款、幂等回放或安全保险丝语义。

## 定向验证结果

- `python -m unittest tests.test_chat_route`：37 项通过。
- `python -m unittest tests.test_chat_db`：27 项通过。
- `python -m unittest tests.test_chat_audit`：5 项通过。
- `python -m unittest tests.test_chat_cleanup`：3 项通过。

### S9 R1 语料工程落地

- 从 `.external/bazi-ishengmind/skills/isheng-mind/data/` 读取 MIT 来源古籍文本，不提交外部 checkout。
- 新增 `tools/build_classics_corpus.py`，按书清洗为 `data/classics/*.jsonl`，并生成 `data/classics/LICENSES.md`。
- 清洗规则显式剥离 `白话译文`、`关键词`、`现代启示`、luckclub 水印、页码与目录段，只保留公版原文。
- 生成结果：7 本、1025 chunks、1,185,549 字；`source_file` 使用脱敏 slug 文件名，不保留 `luckclub` 来源文件名。

#### 对抗性审查

- 先前 parser 曾误删 `原 文` 锚点，已修正为只跳过页面噪音，不跳过正文入口。
- 章节号兼容 `第 1 0 章` 这类数字间空格，避免丢失两位数章节。
- 残留扫描覆盖正文与 `source_file`，确认 `luckclub`、`白话译文`、`关键词`、`现代启示` 等均为零残留。

### S10 R2 检索基线

- 新增 `classics_search.py`：本地 BM25 + CJK bigram/trigram + 命理领域词加权，返回 `chunk_id`、书名、篇名、命中词、摘录。
- 新增 `tools/build_classics_index.py`：构建/冒烟本地检索索引，可按查询输出 top-k 命中。
- 新增 `tools/eval_classics_search.py`：20 组固定问题评估“问题 → 应命中文献”，top-5 命中率当前 100%。
- 新增 `tests/test_classics_search.py`，覆盖精确术语排序、书名过滤、空查询、默认语料水印/译文残留边界。

#### 对抗性审查

- R2 阶段只建立检索基建，未急着接入 `/api/chat`；R3 已在后续小步接入并补 grounding 测试。
- 两个初始 eval case 暴露“期望书目过窄”：`父母兄弟六亲` 命中《三命通会》/《神峰通考》合理，`顺逆旺衰` 命中《滴天髓》原文合理；已修正评估口径，而不是硬调排序刷分。
- 目前仍是词法检索 baseline，不冒充向量/RRF 混合检索；embedding 放到 RAG 增强阶段。

### S11 R3 工具切换 + grounded citation baseline

- `chat_tools.build_chat_tools()` 已把古籍工具从 `lookup_classics` 切到 `search_classics(query, k=4)`，返回 `chunk_id`、可引用格式、书名、篇名、命中词与摘录。
- `chat_graph.build_chat_contract()` 已要求古籍义理/原文出处先调 `search_classics`；引用原文或篇名必须附 `【chunk_id】`。
- 新增 `chat_graph.extract_chunk_ids()`、`extract_retrieved_chunk_ids()`、`validate_grounded_citations()`，用于校验“模型引用 ID ⊆ 本轮检索返回 ID”。
- `/api/chat` 流式循环会收集 `search_classics` ToolMessage 中的 chunk_id；若模型输出未检索过的 chunk_id，即使跨 chunk 拼接，也会截断并替换为 `CHAT_GROUNDING_FALLBACK`，不退款但记录 `status='safety'`。
- 前端 `CHAT_TOOL_HINTS` 增加 `search_classics`，保留 `lookup_classics` 映射以兼容旧事件/历史。

#### 对抗性审查

- grounding 保险丝只认 `search_classics` ToolMessage 返回的 ID；其他工具消息里出现的 chunk_id 不会被误认为检索依据。
- `【fake-` + `book-ch001】` 跨 chunk 输出不会泄露完整伪造 ID，buffer 会保留未闭合 `【...` 等待校验。
- 对高风险查询（如“滴天髓稳赚不赔原文”），`search_classics` 直接返回查无此文，不让“书名 + 原文”这类强词命中掩盖危险诱导。
- 当前仍是 baseline：词法检索 + ID 回验已接入；向量检索、RRF 仍留到后续 RAG 增强。

### S12 R4 离线抽审引用真实性维度

- `chat_requests` 新增 `retrieved_chunk_ids_json` 字段，只保存本轮 `search_classics` 检索返回的 chunk_id 白名单，不保存工具正文、用户问题、命盘 JSON 或生辰信息。
- `db.save_chat_reply()` 增加可选 `retrieved_chunk_ids` 参数，写入前去重、转小写、过滤非法字符；旧调用方默认写入 `[]`，旧库通过幂等迁移补列。
- `db.get_chat_reply()` 与 `list_chat_audit_candidates()` 会返回解析后的 `retrieved_chunk_ids`，供回放检查与离线抽审使用。
- `chat_audit.audit_reply_text()` 复用 `chat_graph.validate_grounded_citations()`；回复中的 `chunk_id` 若不属于本轮检索白名单，会标记 `unverified_classic_chunk_id`。
- `/api/chat` 正常完成与断流保存路径都会把本轮检索到的 `chunk_id` 传给幂等表，让离线抽审能复核原始 turn 的引用依据。

#### 对抗性审查

- 审计日志仍不保存回复正文，`chat_audit_logs` 只存 issue code 与回复哈希；伪造的 `fake-book-ch001` 不会进入 `issue_codes_json`。
- 无引用回复不会被误伤；引用已检索 ID 的回复通过；引用未检索 ID 的回复失败并打 `unverified_classic_chunk_id`。
- 旧数据默认 `retrieved_chunk_ids_json='[]'`，历史回复如含 chunk_id 会被视为“无法验证”，不会被误判为已验证出处。

## R1/R2 定向验证结果

- `python -m unittest discover -s tests -p "test_classics_corpus.py"`：2 项通过。
- `python -m unittest discover -s tests -p "test_classics_search.py"`：4 项通过。
- `python -m py_compile classics_search.py tools/eval_classics_search.py tools/build_classics_index.py tests/test_classics_search.py`：通过。
- `python tools/build_classics_corpus.py --dry-run`：7 本 / 1025 chunks / 1,185,549 字。
- 语料残留扫描：7 文件 / 1025 chunks / 1,185,549 字，`bad=[]`。
- `python tools/eval_classics_search.py --k 5`：20/20，通过率 100%。

## R3 定向验证结果

- `.venv\Scripts\python.exe -m py_compile app.py chat_graph.py chat_tools.py tests/test_chat_route.py tests/test_chat_graph.py tests/test_chat_tools.py tests/test_chat_personas.py tests/test_persona_m5_adversarial.py`：通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_tools.py"`：19 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_graph.py"`：27 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_route.py"`：39 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_personas.py"`：15 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_frontend_persona_contract.py"`：5 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_persona_m5_adversarial.py"`：6 项通过。

## R4 定向验证结果

- `.venv\Scripts\python.exe -m py_compile db.py app.py chat_audit.py chat_graph.py chat_tools.py`：通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_db.py"`：28 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_audit.py"`：9 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_route.py"`：39 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_graph.py"`：27 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_tools.py"`：19 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_cleanup.py"`：3 项通过。
- `git diff --check`：无空白错误，仅 Windows 行尾提示。
- PAT/secret 扫描：GitHub token 常见前缀无命中。

### S13 P4-1 chat helpful 采集闭环

- 新增 `chat_feedback` 表，字段限定为 fingerprint、hid、thread_id、request_id、rating、created_at；同一回复可改投一次最终评分。
- 新增 `db.save_chat_feedback()`、`get_chat_feedback()`、`get_chat_feedback_stats()`，后台 `get_stats()` 增加 helpful 汇总。
- 新增 `/api/chat/feedback`：必须登录、必须拥有该 hid、必须评价已完成落库的 chat reply，rating 仅允许 `1` 或 `-1`。
- 前端 chat 回复收到 done 后显示“有用/没用”按钮，提交时带完成事件的 `request_id` 与发送时锁定的 `activeHid`。
- 删盘级联和 retention 清理同步清理/参考 `chat_feedback`，避免孤儿反馈或误删仍有新反馈的会话。
- docker-compose 热挂载补上 `classics_search.py`、`chat_audit.py` 与 `data/classics`，避免 RAG 新文件在 compose 热替换场景下缺失。

#### 对抗性审查

- 未完成或不存在的 request_id 不能评价，避免刷无效 helpful。
- 他人账号不能用自己的 token 给别人的 hid 写反馈，越权返回 404。
- `chat_feedback` 不含 message、reply_text、paipan_json、birth_info、solar_date 等敏感列。
- 前端提交使用发起请求时的 `activeHid`，减少流式期间切盘导致反馈写到当前盘的风险。

### S14 P4-UI 用户侧咨询间修订

- 保留现有 Flask inline 前端栈，不切 React；在 `app.py` 内把首页从 Agent Console 修订为“命盘咨询间”：顶部 hero、左侧生辰输入/历史、中间继续追问、底部咨询说明。
- 追问区移动到 `resultSection` 的最前面，排盘后用户先看到“继续追问”，再看原始八字表和报告型内容；空状态 `agentEmptyState` 在 `displayResult()` 时隐藏。
- 不再向用户显式展示 `search_classics`、`chunk_id 回验`、`离线抽审`、P4 或 grounding 等工程词；可信度机制改写成“典籍参考、依据、使用边界”。
- 新增/更新前端静态契约测试：用户侧咨询 shell 存在、chat 先于表格、空状态隐藏、后台词不可见、移动端 overflow guard 和 chat 发送按钮宽度重置。

#### 对抗性审查

- 未重写前端框架，避免 P4 阶段把风险扩大到构建链路；保留 `chatSection`、`personaBar`、`resultSection`、`aiGetBtn` 等既有 ID/函数契约。
- 对抗性 UX 结论：工程透明度不等于用户信任。`运行面板`、`grounding`、`chunk_id`、`内测指标` 放在 C 端首页会显得像后台，应只在受保护后台和技术文档中出现。
- 第一次浏览器检查发现 375px 排盘后横向溢出：先给 Agent 工作台子项加 `min-width:0/max-width:100%`，再重置 `.chat-send-btn` 的 `width:auto; margin-top:0; flex:0 0 auto`，最终 overflow=0。
- 保留“每个命盘赠 10 炷香火、各道长每问耗香不同”的商业规则文案，同时追加典籍参考与使用边界说明，避免 UI 只强调体验不解释成本。
- Playwright 使用本机 Chrome 实跑排盘流程，不只做静态字符串检查；桌面和移动截图均确认 chat 位于表格前，底部说明卡片不挤压主咨询区。

## P4 定向验证结果

- `.venv\Scripts\python.exe -m py_compile db.py app.py tests/test_chat_feedback.py`：通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_feedback.py"`：7 项通过。
- `.venv\Scripts\python.exe -m py_compile app.py tests/test_frontend_persona_contract.py`：通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_frontend_persona_contract.py"`：10 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_route.py"`：39 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_db.py"`：28 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_chat_cleanup.py"`：4 项通过。
- `.venv\Scripts\python.exe -m unittest discover -s tests -p "test_admin_stats.py"`：3 项通过。
- Playwright + 本机 Chrome：1440/900/375 宽度打开首页并执行排盘，排盘后 `chatBeforeBazi=true`、`agentEmptyState=none`、横向 overflow=0；截图在 `artifacts/ui-user-consultation/`。
- `docker compose config`：未执行成功，本机未安装 Docker CLI；compose 挂载项已做代码层更新，需在有 Docker 的环境复验。

## 未完成与下一步

- 按你的要求，本轮先不跑全量测试；最后统一做 full unittest、preflight 与 smoke。
- P3/R1/R2/R3/R4 baseline 已完成；P4 已接入 helpful 采集、统计与用户侧命盘咨询首屏。
- 下一步是 P4-2：生成/发放 50-100 个体验码，记录激活与 20+ 真实追问样本；LLM-as-judge、embedding/RRF 与 checkpoint 回滚可作为后续增强。
